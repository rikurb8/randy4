{
  "name": "randy-assistant",
  "version": "1.0.0",
  "description": "Randy AI Assistant Services",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "test": "vitest run --silent",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage"
  },
  "dependencies": {},
  "devDependencies": {
    "typescript": "^5.0.0",
    "vitest": "^0.34.0",
    "@vitest/coverage-v8": "^0.34.0"
  }
}
